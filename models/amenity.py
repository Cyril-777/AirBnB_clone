#!/usr/bin/python3
""" subclass to define the amenity """

from models.base_model import BaseModel


class Amenity(BaseModel):
    """subclass to define the Amenity and its attributes"""
    name = ""
